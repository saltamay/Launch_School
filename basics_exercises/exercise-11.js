// What value does the following expression evaluate to?

'foo' === 'Foo'

// Evaluates to false beause strict comparison (e.g., ===) is only true if the operands are of the same type and the contents match.